type Post {
    id: ID!
    title: String!
    slug: String!
    content: String!
    publish_at: DateTime!
    featured_image: Media @method(name: "featuredImage")
    author: Author @belongsTo
    categories: [Category] @morphMany
}
